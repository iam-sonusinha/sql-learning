# Contributing

Thanks for helping! Please follow these steps:

- 🐛 Bug reports: open an issue with a short reproducible example.
- ✨ New content: create a branch `feat/<short-desc>` and a PR to `main`.
- 🔁 Formatting: follow file naming conventions: `NN_short-description.sql` or `project-xx-name/`.
- ✅ Tests: include expected results in `results/` when adding queries.
- Style: Use clear, commented SQL. Add a short description at top of `.sql` files.

Maintainers will review within a few days. Respect the Code of Conduct.
